<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Admin - Queries</title>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet" />
  <link href="/css/style.css" rel="stylesheet" />
</head>
<body class="font-sans bg-gray-100 site-root">
  <%- include('partials/navbar-admin') %>

  <div class="site-content">
    <main class="container mx-auto px-4 py-10">
      <section class="bg-white shadow-md rounded-lg p-6">
        <h1 class="text-2xl font-semibold mb-4">Queries</h1>
        <% if (queries && queries.length) { %>
          <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
            <% queries.forEach(function(q){ %>
              <% const isCompleted = q.status === 'completed'; %>
              <div class="border rounded-lg p-5 bg-white shadow-sm hover:shadow-md transition">
                <div class="flex items-start justify-between mb-3">
                  <div>
                    <div class="text-xs text-gray-500"><%= new Date(q.createdAt).toLocaleString() %></div>
                    <div class="mt-1 font-semibold text-gray-900">Topic: <span class="font-normal text-gray-800"><%= q.topic %></span></div>
                  </div>
                  <span class="ml-4 h-6 inline-flex items-center text-[11px] font-semibold px-2.5 py-1 rounded-full border uppercase tracking-wide <%= isCompleted ? 'bg-green-100 text-green-800 border-green-200' : 'bg-orange-100 text-orange-800 border-orange-200' %>">
                    <%= isCompleted ? 'Completed' : 'Waiting' %>
                  </span>
                </div>

                <% if (typeof q.etaMinutes !== 'undefined' && q.etaMinutes !== null) { %>
                  <div class="inline-flex items-center text-xs text-blue-800 bg-blue-50 border border-blue-200 rounded-full px-2 py-0.5 mb-2">Current ETA: <span class="ml-1 font-semibold"><%= q.etaMinutes %> min</span></div>
                <% } %>

                <% if (typeof q.rating !== 'undefined' && q.rating !== null) { %>
                  <div class="text-xs text-gray-700 mb-2 flex items-center space-x-2">
                    <span class="text-gray-600">User rating:</span>
                    <span class="text-yellow-500 text-base">
                      <% for (var i = 1; i <= 5; i++) { %>
                        <%= i <= q.rating ? '★' : '☆' %>
                      <% } %>
                    </span>
                    <span class="text-gray-500">(<%= q.rating %>/5)</span>
                  </div>
                <% } %>

                <% if (q.__house) { %>
                  <div class="text-sm text-gray-700 mb-2">
                    <div><span class="font-medium">Address:</span> <%= q.__house.houseNumber %> <%= q.__house.address1 %>, <%= q.__house.suburb %>, <%= q.__house.city %></div>
                  </div>
                  <div class="mb-3">
                    <% if (q.__house.imagePath) { %>
                      <img src="<%= q.__house.imagePath %>" alt="House image" class="w-full h-40 object-cover rounded" />
                    <% } else { %>
                      <div class="w-full h-40 flex items-center justify-center bg-gray-100 text-gray-500 rounded">No image</div>
                    <% } %>
                  </div>
                <% } %>

                <div class="text-sm text-gray-700">
                  <div><span class="font-medium">User:</span> <%= (q.firstName || '') %> <%= (q.lastName || '') %></div>
                  <% if (q.email) { %><div><span class="font-medium">Email:</span> <%= q.email %></div><% } %>
                  <% if (q.phone) { %><div><span class="font-medium">Phone:</span> <%= q.phone %></div><% } %>
                  <% if (q.details) { %><div class="mt-1"><span class="font-medium">Details:</span> <%= q.details %></div><% } %>
                </div>

                <form action="/admin/queries/<%= q._id %>" method="POST" class="mt-3 space-y-3">
                  <div class="grid grid-cols-1 md:grid-cols-2 gap-3">
                    <div>
                      <label class="block text-sm font-medium text-gray-700 mb-1">Status</label>
                      <select name="status" class="w-full px-3 py-2 border rounded-lg bg-white focus:outline-none focus:ring-2 focus:ring-blue-600">
                        <option value="waiting" <%= q.status === 'waiting' ? 'selected' : '' %>>Waiting</option>
                        <option value="completed" <%= q.status === 'completed' ? 'selected' : '' %>>Completed</option>
                      </select>
                    </div>
                    <div>
                      <label class="block text-sm font-medium text-gray-700 mb-1">ETA (minutes)</label>
                      <input type="number" min="0" name="etaMinutes" value="<%= typeof q.etaMinutes !== 'undefined' && q.etaMinutes !== null ? q.etaMinutes : '' %>" class="w-full px-3 py-2 border rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-600" />
                    </div>
                  </div>
                  <button type="submit" class="bg-blue-600 text-white px-4 py-2 rounded-lg font-semibold hover:bg-blue-700">Update</button>
                </form>
              </div>
            <% }) %>
          </div>
        <% } else { %>
          <p class="text-gray-600">No queries found.</p>
        <% } %>
      </section>
    </main>
  </div>

  <%- include('partials/footer') %>
  <script src="/scripts/script.js"></script>
</body>
</html>
